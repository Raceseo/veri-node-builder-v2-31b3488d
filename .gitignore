# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.gstack/
!.gstack/no-test-bootstrap

# .env는 Lovable이 덮어쓰는 것을 막기 위해 git에서 제외
# (docs/lovable-supabase-sync.md, CLAUDE.md 참조)
# 신규 세팅 시: cp .env.example .env
.env

# Supabase CLI 로컬 작업 파일 (프로젝트 ref 등) — 커밋 대상 아님
supabase/.temp/

# 로컬 전용 — 커밋 금지 (환경변수 백업·AI 도구 설정·Supabase 임시)
.env.bak_seoulfix
.claude/
