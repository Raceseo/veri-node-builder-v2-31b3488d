/**
 * 브랜드 카피 단일 출처.
 * ----------------------------------------------------------------------------
 * 🔴 같은 문구가 여러 화면에 하드코딩되면 반드시 갈라진다.
 *    실제로 2026-08-22 에 3곳 중 2곳만 고쳐 30분 만에 갈라졌다.
 *    → 문구를 바꿀 때는 **이 파일만** 고친다.
 *
 * 판별 기준(무엇을 여기 둘 것인가):
 *   "나중에 값이 바뀌면 다른 곳도 고쳐야 하나?"
 *   고쳐야 하면 여기, 한 곳에서만 쓰면 그 파일에 둔다.
 *   → 헤드라인은 IntroView 한 곳뿐이라 여기 두지 않았다.
 */

/**
 * 부제(태그라인) — **카테고리**를 밝히는 자리다.
 *
 * 부제 = "이게 뭐냐"(카테고리) / 헤드라인 = "나에게 뭐가 좋냐"(이득).
 * 역할이 다르므로 **같은 말을 반복하지 않는다** — 반복하면 힘이 분산된다.
 *
 *   앞 "설문 리워드"        = 경쟁사(오베이·패널나우·패널파워)와 같은 카테고리.
 *                            검색되고 3초 안에 이해되는 말이어야 한다.
 *   뒤 "팔리면 또 받는 곳"  = 차별점. 경쟁사는 1회성 보상뿐이라 못 쓰는 말이다.
 *
 * 이력: "나의 데이터 주권을 지키다"(추상적) → "데이터 신탁 플랫폼"(업계 용어,
 *       검색·이해 모두 안 됨) → 현재(2026-08-22).
 *
 * 사용처: IntroView(랜딩) · Auth(가입/로그인)
 */
export const BRAND_TAGLINE = "설문 리워드 · 팔리면 또 받는 곳";

/**
 * 짧은 부제 — **앱 헤더 전용**. 상수가 둘인 이유를 남긴다.
 *
 * 왜 나눴나: 390px 헤더에서 부제에 남는 폭이 약 106px 뿐이다
 * (좌: 퀵메뉴+로고 아이콘, 우: 「기업 공급자 전환」 알약 + 알림). 14자 전문은
 * 10px 에서 ~140px 라 두 줄로 깨진다 — "…또 받는 / 곳".
 * 폰트를 9px 로 줄여도 ~126px 라 여전히 넘치므로 축소로는 해결되지 않는다.
 *
 * 왜 앞부분만 남겼나: 헤더는 설득하는 자리가 아니라 "지금 어느 앱인가"를
 * 알리는 자리다. 카테고리("설문 리워드")만 있으면 그 역할은 끝난다.
 * 차별점("팔리면 또 받는 곳")은 설득이 필요한 랜딩·가입에서 전문으로 말한다.
 *
 * 🔴 둘 중 하나만 고치면 갈라진다. 카테고리 표현을 바꿀 때는 **둘 다** 고친다.
 *    (BRAND_TAGLINE 의 앞 절 = BRAND_TAGLINE_SHORT 여야 한다)
 *
 * 사용처: VeriNodeLogo(앱 헤더 · 로딩 · 결과 화면)
 */
export const BRAND_TAGLINE_SHORT = "설문 리워드";
